<Window x:Class="SignalRClientWpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SignalRClientWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="LightSalmon">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Width="200">
            <TextBlock Text="Введите своё имя" FontSize="20" FontFamily="Impact" HorizontalAlignment="Center"/>
        </Border>
        <Border BorderBrush="Brown" BorderThickness="2" Margin="0 5 0 0 ">
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontSize="20" FontFamily="Impact"/>
        </Border>
        <Border BorderBrush="Brown" BorderThickness="2" Margin="0 5 0 0 ">
            <Button Command="{Binding ServerConnectCommand}" Background="Salmon" FontSize="20" FontFamily="Impact" Content="Присоединиться"/>
        </Border>
    </StackPanel>
</Window>
